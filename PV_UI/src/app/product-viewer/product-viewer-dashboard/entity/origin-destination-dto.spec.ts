import { OriginDestinationDto } from './origin-destination-dto';

describe('OriginDestinationDto', () => {
  it('should create an instance', () => {
    expect(new OriginDestinationDto()).toBeTruthy();
  });
});
