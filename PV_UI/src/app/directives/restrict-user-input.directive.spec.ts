import { RestrictUserInputDirective } from './restrict-user-input.directive';

describe('RestrictUserInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictUserInputDirective();
    expect(directive).toBeTruthy();
  });
});
