import { SwireButtonDirective } from './swire-button.directive';

describe('SwireButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SwireButtonDirective();
    expect(directive).toBeTruthy();
  });
});
